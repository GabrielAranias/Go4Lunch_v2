<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Go4Lunch_v2" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red_primary</item>
        <item name="colorPrimaryVariant">@color/red_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/deep_orange_light</item>
        <item name="colorSecondaryVariant">@color/deep_orange_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="LoginTheme" parent="FirebaseUI">
        <item name="android:windowBackground">@drawable/go4lunch</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="colorPrimary">@color/red_primary</item>
        <item name="android:textColor">@color/red_dark</item>
        <item name="android:textColorPrimary">@color/red_dark</item>
        <item name="android:textColorSecondary">@color/red_dark</item>
    </style>

    <style name="FirebaseUI.TextInputLayout" parent="LoginTheme">
        <item name="colorControlNormal">@color/red_primary</item>
        <item name="colorControlHighlight">@color/red_primary</item>
        <item name="colorControlActivated">@color/red_primary</item>
        <item name="boxBackgroundColor">@color/blue_grey</item>
    </style>
</resources>